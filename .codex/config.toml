# Codex reads the repository-root AGENTS.md automatically.
# Shared project rules live there; MCP connections remain available as needed.

[features]
multi_agent = true

[mcp_servers.github]
url = "https://api.githubcopilot.com/mcp/"
bearer_token_env_var = "GITHUB_PERSONAL_ACCESS_TOKEN"

[mcp_servers.playwright]
command = "bunx"
args = ["--bun", "@playwright/mcp@latest", "--isolated"]
